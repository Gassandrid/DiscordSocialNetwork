[project]
name = "discordsocialnetwork"
version = "0.1.0"
description = ""
authors = [
    {name = "Gassandrid",email = "pedersenewan@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "webdriver-manager (>=4.0.2,<5.0.0)"
]

[tool.poetry]
packages = [{include = "discordsocialnetwork", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
